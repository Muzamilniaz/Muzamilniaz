# Browser URL for the chat backend. The default is the Netlify function below.
VITE_CHAT_API_URL=/.netlify/functions/chat

# Configure these in Netlify environment variables, never as VITE_ variables.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.5
